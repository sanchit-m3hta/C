----------------------1.3 OS structures----------------------

Monolithic structure - Traditional Uninx, Linux
everythig in one place
disadvantage of having everything in one block/system/etc:
- harder to read/comprehend
- debugging is difficult
- recompilation change

advantage of having everything in one block/system/etc:
- fast
- performant

Layered Approach - Android
divided into a numer of layers(levels)
disadvantage:
- slow performance
- difficulty in defining the functionality of each layers
advantages:
modularized code 
simpler debugging
isolation btween layers

Microkernels - Mach influenced Mac OS X's Darwin Kernel
minimal kernel Code: Moves as much from the kernel into user space

Loadable Kernel Modules - Modern UNIX, Linux, Solaris, Windows

kernel Modules 

android is hyrbird architecture becuase:
- portability
- security
- performance

----------------------2.1 Process Introduction----------------------
A process in an operating system is essentially a program in execution. you execute programs.
- program in execution is called a process i.e a running program is a process.
- every function will have a stack associated with it.
- the stack contains temporary data when invoking functions like function paramters, return addresses, local variables (activation record).
-The size of stack memory is determined at compile time, but it is allocated at runtime when functions are called,
    whereas heap memory is allocated dynamically at runtime.
- program counter will not have a history of addresses

Can more than one process be associated with a program? Yes
the text portion the memory will be the same between twp processes,
but data, heap, and stack may not be the same between two processes.
the stack and heap are dynamic, meaning they can grown and shrink


Stack
- The stack does not always decrease.
- It grows when a function is called (new stack frame added).
- It shrinks when a function returns (stack frame removed).
- So it fluctuates: grows and shrinks dynamically as functions are called and return.

Heap
- The heap can grow when memory is allocated dynamically (malloc/new).
- It can shrink when memory is freed (free/delete).
- Its growth/shrinkage depends entirely on your program’s dynamic memory usage.

Q. is it better to use dynamic memory allocation, or just have the whole process on the stack? speed, performance, bloat, bottle-neck

Why Programs run from ram instead of Disk
slower access in Disk. access times from the disk is in micro-seconds, while from the memory is in nanoseconds.

STDIN
STDOUT
STDERR

Practice Questions!! take a look at them

----Multicore processors----
true multiprocessing is only possible with two cores, while you can still do multiprocessing with one core, it won't be as fast.
Processing is split the workload across several cores, depending on tasks and required processsing power.
Core: 
P cores: performance cores: highly performant for heavy processes like ai/ml, video streaming, gaming, vr, etc
E cores: Efficiency cores: highly Efficienct for light processes like word processsing, web browsing, etc.

Revise linked list, arrays, stacks, qeues, in C
- Ready qeue, waiting queue

Q. What is removed from P-core to make an E-core
Big piplines and buffers
    - P cores have very wide out of order instruction pipelines and massive reorder buffers.
       Higher pipelines -> higher througput -> higher performance
    - E cores have narrower pipelines and smaller reorder buffersm which consume less silicon and power.
Cache heirarchy simplified
    - P cores have higher larger private L2 caches and share larger slices of L3
    - E cores ues smaller L2 caches and sometimes skip compex prefetching or aggresive L3 connectivity.
clock speed limits
    - P core are designed for higher frequencies (5+Ghz turbo), while E-cores are optimized for 2-3Ghz

Q. Dynamic allocation of P and E cores for processes.
- first, the process is allocated to a light core i.e E core, then the progress of the process is monitored, caches access,
cach hits and misses, memory access times, etc

Video Game Process:
thread 1: rendeirng
thread 2: physics
thread 3: audio

t1 goes into L1, t2 goes into L2

Context switching

SMT, OS Scheudling
SMT = Simultaneous Multi Threading


Data Leakage Between threads
Becasue two threads on a single core share resources like L3 data cache and other microarchitectural buffers, an attack can use
Side-channel attack to infer or directly read data from the other thread

Side channel attack:
PRIM(?)
WAIT 
Probe

----------------------2.2 Process Lifecycle----------------------
PCB: Process control block

0x123
- i/o event 
- interrupted
- waiting for children to die 

//the textbook is incorrect in terms of qeues

- nobody uses strings for storing passwords in java, because strings are immutable (unchangble) in java
- use char[] "char arrays" instead.


Qeuing diagram
//draw it

----------------------2.3 PCB (Process Control Block)----------------------
info about each process is stored in a PCB
each process is represented in the OS by a PCB
OS maintains a data structure (e.g. list) of all active processes

stages of a process:
running
waiting 
ready 
execution


why is a process stage stored?
- [idk forgot to note down]
- scheduling

when you run out of memory, the OS can either: dynamic allocation, or get more memory i.e virutal memory, 
however not all of the virutal memory is available for us. example virutal memory might be 5MB, but only 3MB might be available
VMRSS - Virtual memory Resident

within linux, al active processes are represented using a doubly linked list of task_struct.
Why doubly linked list?
- much easier while changing the state of a process.
- complexity is O(1) compared to O(n) in sinlgy linked list.
- Better performance

What is forking?
[search on google]

what is COW?
Copy-on-write
[search on google]



PR = Priority, NI = Nice Values
Priority can be fixed, dynamic, and [something something] by Nice Values (NI)
PR = 20 (default)
User-> Nice - NI 
PR = 20 + NI
[ Defaut] Nice value = 0

-100 is the highest Priority
129 is the lowest priority


ps -e 
ps -ef 

fork-exec creates a new prcocess (im not sure if this 100% correct).
fork()
exec()

pid: prcoess id 
ppid: parent process id
uid: user id
euid: effective user id

//daemons
What is a deamon?
A daemon is a background process that runs without direct user interaction.
controlling term
    - foreground: UID is User
    - background: UID is root, and TTY is tty1 (maybe tty2 as well)
    - daemons: UID is root, TTY is "?"

ctrl + c: interrupt
ctrl + z: suspend //moves the process in the backgrond, where it can be resume via "bg"

command: bg //background
       : fg //foreground


zombie child
orphan child
zombie process

return value of the fork, 
if reutrn value is 0, its the child
if you get value of the child,then its parent.


--------------------------------------------------IPC (INTER PROCESS COMMUNICATION)-----------------------------------------
ls | wc -l: lists the number of paths in the pwd 

DIFFERENT WAYS PROCESSES CAN COMMUNICATE:
- signals
- message passing: safe
- shared memory: fast

commands to create a mechanism that allows the above ways for processe to communicate:
shmct
shmatc
shmdt
shmctl

covered up to pipes, page 35. midterm 1: from introduction up to pipes page 35 - IPC


two types of IPC
- direct: name = process ID, SYNCHRONIZATION, fast, straightforward. 
        - signals
        - message passing
        - unnamed pipe: within the same heirarchy, no ID associated with them, half duplex
          remains persistant even after the process has died.
- indirect:
    - named pipes =  FIFO pipe, "makefifo()", full duplex, bleong to the file system
    - message qeues = publishers give message to queue in fifo bassis, diagnostic tool (another process) that reads the messages, data is persistant
    - shared memory (maybe?) 
    - mailboxes
    - sockets = n endpoint for a communication channel, used for both physical and digital connections.

------pipe()------
fd[0] = reading
fd[1] = writing

syntax:
write(fd,buf, count)
read(fd, buf, count)

if either read or write fails, -1 is returned. 

- differentiate betwen a FIFO file and a normal file.
- differentiate between named pipes and unnamed pipes.


named pipes are half duplexs
unamed pipes are not data persistant. but they are persistant in the sense that the pipes remain after the process has terminated.

fifo pipes = p (when invoking ls -l)

Owner/User
GRoup
Others
0644

----FIFO PIPE-----
command to create fifopipe = makefifo()
directives to include while using makefifo()
#include</sys/types.h>
#include<sys/unistd.h>
#inlcude<unistd.h>
#include<fcntl.h>

----------------------3. Thread Introduction-----------------------
Cache: Cache is very small and very fast peice of memory sitting between the cpu and the main memory. 
    - It exits because RAM is very slow compared to the cpu

Cache HIT and MISS:
    - When the CPU needs information, it first checks the cache.
    - if its not there, Cache MISS, and the CPU must fetch from RAM i.e which is slower -> slower performance
    - if its there, Cache HIT -> faster performance

COLD and WARM Cache:
    - When the cache is filled with data unrelated to the current running process: COLD cache. -> more cache MISSES
    - when the cache is contains useful datat related to the current running processs: WARM cache -> more cache HITS

thread GRoup
threads that belong to the same process

---Thread Safe---
Mutexes
semaphores
conditional variables

--------concurrency Vs ||ism-------
Read up on this::

---creating threads----
 pthread p1;    //data type of threads

 pthread_create()       //create threads
 




-------------Deadlock-----------------
the deadlocks get resolved by the process (program), NOT the operating system.

 - Deadlock ignorance
 deadlocks do not get recognized by the OS

- A TESLA hit a police car because of the deadlock.
- self driving cars are the most dangerours places where a deadlock can by deadly
- each sensor is a thread in a TESLA

necesary conditions for a deadlock:
- mutual exclusion
- hold & wait
- no preemption
- circular wait







